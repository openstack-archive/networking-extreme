============
Installation
============

At the command line::

    $ pip install networking-extreme

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-extreme
    $ pip install networking-extreme
